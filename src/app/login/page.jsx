import Login from "@/components/Login";
import React from "react";

export const metadata = {
  title: "CollegeCloud | Login",
};

export default function LoginPage() {
  return (
    <div>
      <Login />
    </div>
  );
}
